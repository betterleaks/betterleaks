// Package limits defines bounds shared by credential discovery and resolution.
package limits

// ComponentSets bounds the Cartesian product retained and analyzed per finding.
const ComponentSets = 100

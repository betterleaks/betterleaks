package report

// New betterleaks branding.
const version = "v0.1.0"
const driver = "betterleaks"

// Legacy gitleaks branding (used by --legacy reporters).
const legacyVersion = "v8.0.0"
const legacyDriver = "gitleaks"

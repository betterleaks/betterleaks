# This file has been auto-generated. Do not edit manually.
# If you would like to contribute new rules, please use
# cmd/generate/config/main.go and follow the contributing guidelines
# at https://github.com/betterleaks/betterleaks/blob/main/.github/CONTRIBUTING.md
#
# How the hell does secret scanning work? Read this:
#   https://lookingatcomputer.substack.com/p/regex-is-almost-all-you-need
#
# This is the default betterleaks configuration file.
# Rules and filters are defined within this file.
# Rules instruct betterleaks on what should be considered a secret.
# Filters instruct betterleaks on what to skip (prefilter) or discard (filter).

title = "{{.Title}}"

# minVersion indicates the minimum gitleaks config format version required.
# betterleaksMinVersion indicates the minimum betterleaks version required.
# If the running version is older, a warning will be logged.
minVersion = "v8.25.0"
betterleaksMinVersion = "v1.7.5"

{{ with .Prefilter }}
prefilter = {{ tomlExpr . }}
{{ end }}{{ with .Filter }}
filter = {{ tomlExpr . }}
{{ end }}
{{- range $i, $rule := .Rules }}{{println}}# ──────────────────────────────────────────────────────────────────────────────
# {{ $rule.RuleID }}
# ──────────────────────────────────────────────────────────────────────────────
[[rules]]
id = "{{$rule.RuleID}}"
description = "{{$rule.Description}}"
{{- with $rule.Regex }}
regex = '''{{ . }}'''{{ end -}}
{{- with $rule.Path }}
path = '''{{ . }}'''{{ end -}}
{{- with $rule.SecretGroup }}
secretGroup = {{ . }}{{ end -}}
{{- if eq $rule.RuleID "generic-api-key" }}
specificity = 0
{{- else if $rule.Specificity }}
specificity = {{ $rule.Specificity }}
{{- end -}}
{{- with $rule.Confidence }}
confidence = "{{ . }}"{{ end -}}
{{- with $rule.Keywords }}
{{- if gt (len .) 1}}
keywords = [{{ range $j, $keyword := . }}
    "{{ $keyword }}",{{ end }}
]{{else}}
keywords = [{{ range $j, $keyword := . }}"{{ $keyword }}"{{ end }}]{{end}}{{ end }}
{{- with $rule.Tags }}
tags = [
    {{ range $j, $tag := . }}"{{ $tag }}",{{ end }}
]{{ end }}
{{- if $rule.SkipReport }}
skipReport = true{{ end }}
{{- with $rule.ValidateExpr }}
validate = {{ tomlExpr . }}{{ end }}
{{- with $rule.Filter }}
filter = {{ tomlExpr . }}{{ end }}
{{- with $rule.Components }}
components = [{{ range . }}
    { id = "{{ .RuleID }}"{{ if .Optional }}, optional = true{{ end }}{{ with .Within }}, within = "{{ . }}"{{ end }} },{{ end }}
]{{ end }}
{{ end }}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Betterleaks v2 finding",
  "description": "One serialized report.Finding, emitted on each JSONL line or in a JSON report array. Discovery supplies match and location; optional analysis describes credential state, identity, and permissions.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "ruleID",
    "description",
    "confidence",
    "match",
    "location",
    "tags"
  ],
  "properties": {
    "ruleID": {
      "type": "string",
      "description": "Identifier of the detection rule."
    },
    "description": {
      "type": "string",
      "description": "Human-readable rule description."
    },
    "confidence": {
      "type": "string",
      "description": "Detection confidence, normally low, medium, high, or an empty string when unclassified. Custom source attributes can supply other strings; this field is intentionally not an enum.",
      "examples": [
        "",
        "low",
        "medium",
        "high"
      ]
    },
    "match": {
      "$ref": "#/$defs/match"
    },
    "matchContext": {
      "type": "string",
      "description": "Requested surrounding context, omitted when empty."
    },
    "attributes": {
      "$ref": "#/$defs/stringMap",
      "description": "Extensible source metadata such as resource, git.sha, or git.author_name. Path is promoted into location.path. fs.first_fragment is omitted; git.message is abbreviated to its first line with ... for further nonblank text."
    },
    "location": {
      "$ref": "#/$defs/location"
    },
    "analysis": {
      "$ref": "#/$defs/analysis",
      "description": "Credential state and optional identity and permission evidence. Omitted when no provider result exists."
    },
    "componentSets": {
      "type": "array",
      "description": "Multipart credential combinations, each with its own analysis result. Omitted when empty.",
      "items": {
        "$ref": "#/$defs/componentSet"
      }
    },
    "tags": {
      "type": [
        "array",
        "null"
      ],
      "description": "Rule and detection tags. Always emitted; a nil Go slice serializes as null.",
      "items": {
        "type": "string"
      }
    }
  },
  "$defs": {
    "stringMap": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Provider-specific JSON data; values may include nested objects, arrays, scalars, or null.",
      "additionalProperties": true
    },
    "location": {
      "type": "object",
      "description": "Source-relative position. Text matches normally use one-based lines and byte columns; unavailable coordinates and paths are omitted. Message coordinates are message-relative.",
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "description": "Optional source-defined path, such as a file, Git object, or archive member."
        },
        "startLine": {
          "type": "integer"
        },
        "endLine": {
          "type": "integer"
        },
        "startColumn": {
          "type": "integer"
        },
        "endColumn": {
          "type": "integer"
        }
      }
    },
    "analysis": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string",
          "description": "Credential liveness result. An empty Go status is omitted; none is a CLI selector, not a serialized status.",
          "enum": [
            "valid",
            "needs_validation",
            "invalid",
            "revoked",
            "unknown",
            "error"
          ]
        },
        "reason": {
          "type": "string",
          "description": "Explanation of the result, including unavailable evidence or analysis failure."
        },
        "severity": {
          "type": "string",
          "description": "Provider-neutral lower bound derived by Betterleaks from positively observed capabilities. Independent of detection confidence and credential validity.",
          "enum": [
            "unknown",
            "low",
            "medium",
            "high"
          ]
        },
        "identity": {
          "$ref": "#/$defs/identity"
        },
        "capabilities": {
          "type": "array",
          "description": "Positively observed grants. An absent capability means unknown, not denied.",
          "items": {
            "type": "string",
            "enum": [
              "read",
              "write",
              "read_secrets",
              "create_credentials",
              "manage_users",
              "admin"
            ]
          }
        },
        "metadata": {
          "$ref": "#/$defs/metadata"
        },
        "debug": {
          "$ref": "#/$defs/metadata",
          "description": "Optional sanitized provider diagnostics, grouped by validation and analysis Expr stage."
        }
      }
    },
    "identity": {
      "type": "object",
      "description": "Available identity of the principal associated with the credential. Fields may be omitted independently.",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string"
        },
        "username": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "account": {
          "$ref": "#/$defs/account"
        }
      }
    },
    "account": {
      "type": "object",
      "description": "One unambiguous account or tenant reached by the credential. Omitted when analysis cannot identify a single account.",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "componentSet": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "components"
      ],
      "properties": {
        "components": {
          "type": [
            "array",
            "null"
          ],
          "description": "One credential combination. Normally an array of component findings; nil SDK slices and pointers serialize as null.",
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/componentFinding"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "analysis": {
          "$ref": "#/$defs/analysis"
        }
      }
    },
    "componentFinding": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ruleID",
        "match",
        "location"
      ],
      "properties": {
        "ruleID": {
          "type": "string"
        },
        "optional": {
          "type": "boolean",
          "description": "Whether this component is optional in the primary rule. Omitted when false."
        },
        "match": {
          "$ref": "#/$defs/match"
        },
        "location": {
          "$ref": "#/$defs/location"
        }
      }
    },
    "match": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "full",
        "value"
      ],
      "properties": {
        "full": {
          "type": "string",
          "description": "Matched text, or a description for a path-only rule. May be redacted."
        },
        "value": {
          "type": "string",
          "description": "Extracted value. May be a non-secret component, empty for path-only findings, or redacted."
        },
        "captures": {
          "$ref": "#/$defs/stringMap",
          "description": "Named regex captures from this match."
        }
      }
    }
  },
  "examples": [
    {
      "ruleID": "example-token",
      "description": "Example token",
      "confidence": "",
      "match": {
        "full": "REDACTED",
        "value": "REDACTED"
      },
      "location": {
        "startLine": 1,
        "endLine": 1,
        "startColumn": 1,
        "endColumn": 20
      },
      "tags": []
    },
    {
      "ruleID": "example-token",
      "description": "Example token with analysis",
      "confidence": "high",
      "match": {
        "full": "REDACTED",
        "value": "REDACTED"
      },
      "attributes": {
        "resource": "file"
      },
      "location": {
        "startLine": 12,
        "endLine": 12,
        "startColumn": 14,
        "endColumn": 53,
        "path": "service/.env"
      },
      "analysis": {
        "status": "valid",
        "severity": "high",
        "identity": {
          "username": "example-user",
          "account": {
            "id": "example-account"
          }
        },
        "capabilities": [
          "read",
          "write"
        ],
        "metadata": {
          "scopes": [
            "example:read",
            "example:write"
          ]
        }
      },
      "tags": []
    }
  ]
}
